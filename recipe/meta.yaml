{% set name = "wwt_jupyterlab_extension" %}
{% set version = "1.3.2" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/wwt_jupyterlab_extension-{{ version }}.tar.gz
  sha256: d15e0f5a819144065921bf3ab0939807ba62de762ac21669bb2f9cb3ff743edd

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - jupyter-packaging
    - pip
    - python >=3.6
  run:
    - python >=3.6

test:
  imports:
    - wwt_jupyterlab_extension
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/WorldWideTelescope/wwt-jupyterlab
  summary: Prebuilt AAS WorldWide Telescope JupyterLab extension
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - pkgw
